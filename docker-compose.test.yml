services:
  db:
    image: postgres:18-alpine
    container_name: fresh-fridge-db-test
    environment:
      POSTGRES_USER: fridge_user
      POSTGRES_PASSWORD: fridge_password
      POSTGRES_DB: fresh_fridge
    ports:
      - "5432:5432"

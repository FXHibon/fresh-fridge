services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: fresh-fridge:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
